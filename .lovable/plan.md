

# خطة تعديل نافذة التحضير في Attendance.tsx

## ملخص
تحديث ثوابت التوقيت، إضافة ألوان ديناميكية للعداد التنازلي (أخضر → أصفر → أحمر)، تسجيل الغياب التلقائي عند الإغلاق، وتحديث النصوص.

## التغييرات في `src/pages/Attendance.tsx`

### 1. تحديث الثوابت (سطر 20-22)
```typescript
const TEACHER_WINDOW_OPEN = 0;      // تفتح عند أذان العصر بالضبط
const TEACHER_WINDOW_CLOSE = 105;   // العصر + 105 دقيقة
const ON_TIME_THRESHOLD = 70;       // العصر + 70 دقيقة = حد التأخر
```

### 2. إضافة state للون العداد (سطر ~41)
```typescript
const [countdownColor, setCountdownColor] = useState("text-primary");
```

### 3. تعديل useEffect للعداد (سطر 83-105)
- حساب `lateThreshold` (العصر + 70 دقيقة) بالإضافة إلى `openTime` و `closeTime`
- عند النافذة مفتوحة:
  - أكثر من 10 دقائق متبقية وقبل مرحلة التأخر → `text-primary` (أخضر)
  - بعد ON_TIME_THRESHOLD وأكثر من 10 دقائق → `text-yellow-600` (أصفر)
  - أقل من 10 دقائق متبقية → `text-red-600` (أحمر)

### 4. تعديل عرض العداد (سطر 474)
استبدال `text-primary` بالمتغير `countdownColor`

### 5. تحديث نصوص بطاقة النافذة (سطر 447-482)
- إزالة "فتح النافذة" (لأنها تفتح عند الأذان مباشرة)
- تغيير "العصر+45د" → "العصر+70د"
- تغيير "العصر+2س" → "العصر+105د"
- تحديث النص التوضيحي (سطر 480): "تفتح النافذة عند أذان العصر وتغلق بعد 105 دقائق"

### 6. تسجيل الغياب التلقائي عند الإغلاق
في `handleSave` (سطر 189-273):
- بعد مرحلة التأخر (70 دقيقة): الطلاب المحددون "حاضر" يُسجَّلون "متأخر"
- عند إغلاق النافذة: الطلاب بدون تسجيل يُسجَّلون "غائب" تلقائياً (ما عدا "معذور")

### 7. تحديث نص حالة التأخر للمعلم (سطر 499)
```
حد التأخر: {formatTimeFromAsr(70)}
```

---

**ملف واحد فقط**: `src/pages/Attendance.tsx`

